SELECT
RANK() OVER(PARTITION BY RANKING BETWEEN 6 AND 10 AND ORDER BY SALARY DESC) RANKING,
E.LAST_NAME, E.FIRST_NAME, E.SALARY
FROM EMPLOYEES E
GROUP BY E.LAST_NAME, E.FIRST_NAME, E.SALARY
ORDER BY SALARY DESC;